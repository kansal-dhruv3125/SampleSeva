import { Router } from "express";

/**
 * User profile endpoints (Phase 5F/5I): GET/PATCH /me, addresses CRUD.
 * Scaffold placeholder — no endpoints implemented yet.
 */
export const usersRouter = Router();
